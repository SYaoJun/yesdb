# cmake_minimum_required(VERSION 3.16)

# include(GoogleTest)

# file(GLOB_RECURSE YESDB_TEST_SOURCES "${PROJECT_SOURCE_DIR}/test/*test.cpp")

# string(REPLACE ".cpp" "" SRC_FILE_NAME "${YESDB_TEST_SOURCES}" )

# foreach(yesdb_test_name ${SRC_FILE_NAME} )
#     add_executable( ${yesdb_test_name} ${yesdb_test_name}.cpp )
#     target_link_libraries(${yesdb_test_name} yesdb gtest gmock_main)
# endforeach()